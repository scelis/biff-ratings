name: Validate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate-ratings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate ratings.json
        run: python3 scripts/validate_ratings.py
